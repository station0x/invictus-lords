name: semidaily-cron
on:
  schedule:
    # - cron: '0 */12 * * *'
    - cron: '*/5 * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call our API route
        run: |
          curl --request POST \
          --url 'https://develop.invictuslords.com/api/tokens/cron' \
          --header 'Authorization: Bearer ${{ secrets.API_SECRET_KEY }}'